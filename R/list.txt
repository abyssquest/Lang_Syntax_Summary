R 공부
# 네이밍 특징 - .이 객체의 멤버로드가 아니라 그냥 글자
# 주석문

available.package()				R 패키지 목록 보기
dim( available.package() )		R 패키지 보기
sessionInfo()					R 세션 보기

installed.packages()			설치된 패키지 보기
search()						메모리 로드된 패키지 보기

install.packages("패키지명")	패키지 설치
library(패키지명)				패키지 메모리 로드

remove.packages("패키지명")		패키지 삭제

패키지 CRUD
패키지 생성
패키지 읽기
패키지 수정
패키지 삭제

패키지 메모리 읽기
패키지 메모리 삭제


변수 CRUD
- 스칼라변수 : 값 1개
	NA(Not Available)
		계산시 na.rm = T 로 제거
	


hist() 히스토그램
lines() 분포곡선


rnorm() 난수 생성
sum()


대입연산자 <-

pdf("경로/파일이름.확장자") pdf로 결과 출력
dev.off() 출력할 파일 닫기